# Lesson 2.1 — Loading &amp; Posing Genesis Figures — Image Requirements

Art/screenshots needed to replace the `.image-placeholder` blocks in `lesson_04_loading_and_posing_figures.html`. Save finished files to `images/` and swap each placeholder `<div class="image-placeholder">…</div>` for an `<img src="images/FILENAME" alt="…">`.

| # | Placeholder location (section) | Suggested filename | Type | Description | Alt text |
|---|-------------------------------|--------------------|------|-------------|----------|
| 1 | §2 "Loading a Figure" | `l04_default_apose.png` | Screenshot | A freshly loaded Genesis 8 figure standing in its neutral default A-pose in the viewport, framed to fill the view. Clean/default lighting is fine. | A freshly loaded Genesis 8 figure standing in its default neutral A-pose |
| 2 | §4 "ActivePose & PowerPose" | `l04_activepose_powerpose.png` | Screenshot (side-by-side) | Left: the **ActivePose** tool mid-drag on a figure's hand in the viewport (show the drag/IK). Right: the **PowerPose** pane with its flat puppet control map. | ActivePose dragging a figure's hand beside the PowerPose puppet control map |

## Notes
- The one Mermaid diagram (§3 posing-workflow flow: load → preset → adjust → pin → save) is **live Mermaid** already in the page — no image needed. It re-themes automatically in dark mode.
- Prefer 16:9 or 4:3 screenshots at ~1600px wide; PNG for UI.
- A **light UI theme** reads better in print. Blur or crop out any account email/username.
- For Figure 2, capturing both tools in one composite is ideal; otherwise supply two images and split the placeholder into two figures.
- Keep alt text descriptive for accessibility (WCAG AA — matches course standard).

## Status
- [x] Figure 1 — default A-pose
- [x] Figure 2 — ActivePose &amp; PowerPose
