# Lesson 1.3 — The Daz Studio Interface — Image Requirements

Art/screenshots needed to replace the `.image-placeholder` blocks in `lesson_03_the_daz_studio_interface.html`. Save finished files to `images/` and swap each placeholder `<div class="image-placeholder">…</div>` for an `<img src="images/FILENAME" alt="…">`.

| # | Placeholder location (section) | Suggested filename | Type | Description | Alt text |
|---|-------------------------------|--------------------|------|-------------|----------|
| 1 | §1 "The Workspace at a Glance" | `l03_workspace_regions.png` | Screenshot (annotated) | Full Daz Studio window with a figure loaded, with the four regions clearly labeled: **Viewport** (center), **Content panes** (Smart Content / Content Library), **Scene pane**, and **Parameters** pane. Use callout boxes/arrows. | Annotated Daz Studio interface with the viewport, content panes, Scene pane, and Parameters pane each labeled |
| 2 | §2 "Navigating the Viewport" | `l03_viewport_controls.png` | Screenshot (close-up) | Zoomed-in on the viewport's top edges: the **view/camera selector** dropdown at top-left and the **DrawStyle** control plus **orbit / pan / zoom** navigation icons at top-right. | Close-up of the viewport corners showing the view selector, DrawStyle control, and navigation icons |
| 3 | §3 "The Scene Pane" | `l03_scene_pane.png` | Screenshot | The **Scene pane** with a Genesis 8 figure expanded to reveal its bone hierarchy, a parented prop (e.g. a hat under the head), and a camera node — showing indentation, node-type icons, and the eye visibility toggle. | The Scene pane tree with a Genesis figure expanded to show its bone hierarchy |
| 4 | §6 "Tabs, Panes & Layouts" | `l03_workspace_menu.png` | Screenshot | The **Window → Workspace** menu open, showing **Select Layout**, **Save Layout As…**, and the built-in layout list (e.g. **City Limits**, **Hollywood Blvd**). | The Window → Workspace menu open, showing Select Layout and Save Layout As with the built-in layout list |

## Notes
- The two Mermaid diagrams (§1 workflow loop, §6 customize-and-save flow) are **live Mermaid** already in the page — no image needed. They re-theme automatically in dark mode.
- Prefer 16:9 or 4:3 screenshots at ~1600px wide; PNG for UI.
- A **light UI theme** reads better in print. Blur or crop out any account email/username.
- Keep alt text descriptive for accessibility (WCAG AA — matches course standard).
- For Figure 1, capture with a figure already loaded so all four regions have real content to point at.

## Status
- [x] Figure 1 — workspace regions (annotated)
- [x] Figure 2 — viewport controls close-up
- [x] Figure 3 — Scene pane with figure expanded
- [x] Figure 4 — Window → Workspace menu
