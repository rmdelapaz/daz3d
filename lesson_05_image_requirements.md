# Lesson 2.2 — Shaping Characters with Morphs — Image Requirements

Art/screenshots needed to replace the `.image-placeholder` blocks in `lesson_05_shaping_characters_with_morphs.html`. Save finished files to `images/` and swap each placeholder `<div class="image-placeholder">…</div>` for an `<img src="images/FILENAME" alt="…">`.

| # | Placeholder location (section) | Suggested filename | Type | Description | Alt text |
|---|-------------------------------|--------------------|------|-------------|----------|
| 1 | §2 "The Shaping Pane" | `l05_shaping_pane.png` | Screenshot | The Daz Studio **Shaping** pane with a Genesis 8 figure selected, showing the category list (Actor / Head / Body / Expressions) and a group of morph dials expanded. | The Shaping pane with a Genesis figure selected, showing Actor, Head, and Body groups and their morph dials |
| 2 | §4 "Shaping the Head & Face" | `l05_face_before_after.png` | Screenshot (side-by-side) | Before/after of the same figure's head: **left** the default base head, **right** the same head after dialing custom eye, nose, and jaw morphs. Frame tight on the head at eye level; identical camera and lighting for both halves. | Before-and-after of a Genesis figure's face — default base head on the left, custom head morphs applied on the right |

## Notes
- Both Mermaid diagrams (§2 morph-category map; §3 full-character workflow) are **live Mermaid** already in the page — no image needed. They re-theme automatically in dark mode.
- Prefer 16:9 or 4:3 screenshots at ~1600px wide; PNG for UI.
- A **light UI theme** reads better in print. Blur or crop out any account email/username.
- For Figure 2, a single composite is ideal; otherwise supply two matched images (same camera/lighting) and split the placeholder into two figures.
- Keep alt text descriptive for accessibility (WCAG AA — matches course standard).

## Status
- [x] Figure 1 — Shaping pane (Actor / Head / Body)
- [x] Figure 2 — face before &amp; after head morphs
